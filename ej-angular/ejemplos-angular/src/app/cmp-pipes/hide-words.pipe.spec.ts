import { HideWordsPipe } from './hide-words.pipe';

describe('HideWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new HideWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
